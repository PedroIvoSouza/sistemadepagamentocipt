{
  "name": "Node.js & SQLite for CIPT",
  "image": "mcr.microsoft.com/devcontainers/javascript-node:18",
  "postCreateCommand": "sudo apt-get update && sudo apt-get install -y libatk1.0-0 libatk-bridge2.0-0 libcups2 libgtk-3-0 libgbm-dev libnss3 libasound2 && npx puppeteer browsers install chrome",
  "forwardPorts": [3000],
  "containerEnv": {
    "ADMIN_BASE_URL": "https://admin.portalcipt.com.br"
  },
  "customizations": {
    "vscode": {
      "extensions": [
        "ms-azuretools.vscode-docker",
        "dbaeumer.vscode-eslint"
      ]
    }
  }
}